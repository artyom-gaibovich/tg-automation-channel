# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @tg-automation-channel/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tg-automation-channel-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tg-automation-channel-backend && \
          adduser --system -G tg-automation-channel-backend tg-automation-channel-backend

COPY apps/backend/dist tg-automation-channel-backend/
COPY apps/backend/package.json tg-automation-channel-backend/
RUN chown -R tg-automation-channel-backend:tg-automation-channel-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tg-automation-channel-backend --omit=dev -f install

CMD [ "node", "tg-automation-channel-backend" ]
